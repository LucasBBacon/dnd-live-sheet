{
  "name": "dnd-live-sheet",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "turbo run dev",
    "build": "pnpm check:hygiene && turbo run build",
    "lint": "pnpm -r --if-present lint",
    "check:hygiene": "node ./scripts/check-source-hygiene.mjs",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "test:shared": "pnpm --filter @project/shared test",
    "test:all": "pnpm check:hygiene && pnpm --filter @project/shared test --run && pnpm --filter @project/database test --run && pnpm --filter @project/server test --run && pnpm --filter @project/web test --run",
    "test:coverage": "pnpm --filter @project/shared test:coverage && pnpm --filter @project/database test:coverage && pnpm --filter @project/server test:coverage && pnpm --filter @project/web test:coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.8.0",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.0.0",
    "eslint": "^10.3.0",
    "globals": "^17.6.0",
    "turbo": "^2.9.18",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.2"
  }
}
